{
  "version": 2,
  "buildCommand": "ng build -c production && ng run rapid-pharmaceuticals:server",
  "functions": {
    "api/index.js": {
      "runtime": "nodejs20.x"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.js" }
  ]
}